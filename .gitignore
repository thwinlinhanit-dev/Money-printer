# Rust build artifacts
/target
**/*.rs.bk

# Real config & secrets (only *.example.* is committed) — PD-2
*.env
.env
.env.*
!.env.example
!*.example.toml
config.toml
secrets*

# Recorded market data & run artifacts — W-6 (never commit data)
/data
/data-migrated
/raw
/cold
/warm
/runs
/research/out
/journal/briefs
/journal/telegram
/journal/cohort_changes.jsonl
*.parquet
*.ndjson.zst
*.log
*.eventlog

# Local scratch (ad-hoc binaries/inspectors — hygiene, audit L-series)
.freebuff/
/scratch
/bin_local/
scratch_*
stdout.txt
stderr.txt
collector_stdout.log
collector_stderr.log
.omx/
.pytest_cache/
__pycache__/
*.pyc
*.pdb
research/py_err.txt
research/py_out.txt

# Test-artifact data written under a crate dir by materialize/query tests
# (W-6: recorded/test data never enters git; root /data is covered above).
/storage/data/

# Off-host backup tier — vendored portable binaries + age keys (PD-2)
ops/tools/
ops/keys/
ops/offhost-drill/
data/.offhost-staging/
data/.offhost-drill/

# TradingView MCP server (third-party, not committed)
tradingview-mcp/
.npm-cache/
