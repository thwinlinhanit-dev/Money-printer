# Full-System Audit — 2026-08-06

Independent full-workspace audit of Money Printer (10 Rust crates + `research/`
Python + specs + ops + live Phase-0 recorder). Method: fresh baseline
verification on this host, re-check of every 08-04 FIXED claim against code,
secrets/hygiene/guardrails sweeps, operational health of the live collectors
and scorecards, and a residual-risk pass over decision-path crates.

**Scope:** tree at branch `feature/signal-catalog-footprint`, HEAD `1e97ff0`,
plus a **dirty working tree** (~32 modified + 5 untracked files, including
uncommitted `mp-materialize`).

---

## Baseline (verified this session)

| Check | Result |
|---|---|
| `cargo check --workspace` | clean |
| `cargo test --workspace` | **all green** — 368 Rust tests, 0 failures, 0 ignored, 0 doc-test failures |
| `py -3 -m pytest research/tests` | **49/49 pass** (`python` not on PATH; use `py -3`) |
| `cargo clippy --workspace --all-targets -- -D warnings` | clean |
| `ops/ci/guardrails.ps1` | **all checks passed** |
| Secrets / credentials sweep | clean (PD-2) — only env-var *names*, fixtures (`SECRET_KEY`), and docs |
| Committed `.env` / real `config.toml` / key material | none found |
| PD-1 (never live) | structurally clean — no venue trading adapter, no credentials in code |

---

## Prime-Directive posture

| Directive | Verdict | Notes |
|---|---|---|
| **PD-1** Never live | PASS structurally | No execution venue adapter. `TradingMode::Live` remains constructible via `MONEY_PRINTER_MODE=live` / host `mode.toml` (convention only — no compile-time barrier). |
| **PD-2** No secrets | PASS | Guardrails + manual sweep clean. |
| **PD-3** Determinism | PASS on decision paths | Wall clock confined to `core/src/wall_clock.rs` + infra; strategies/features/sim use injected clock. |
| **PD-4** Strategies ≠ venues | PASS | No venue/credential deps in strategies/features. |
| **PD-5** Honesty over green | PASS | No `#[ignore]`, no loosened tolerances found. |
| **PD-6** Spec before code | PASS in spirit | Specs 000–031 present; uncommitted materialize work is documented in `POST_CLEAN_PLAN` / spec 016. |

---

## Prior-audit (08-04) FIXED-claim verification

Re-checked against current code (not re-trusted from docs alone):

| Item | Status |
|---|---|
| opsd token auth + 5s read timeout + non-loopback fail-closed | ✅ `ops/src/bin/opsd.rs` |
| gate RG-0 finiteness of position/gross/PNL | ✅ `risk/src/gate.rs` + `regression_gate_nan_state_inputs_rejected` |
| promotion + INT-4 `compact_day_verified` | ✅ storage + mp-ops |
| IntentId engine namespacing + multi-strategy dispatch | ✅ sim engine + tests |
| reseed backoff (COL-1) | ✅ mp-collector |
| watchdog symbol allowlist + quoting | ✅ `watchdog_collectors.ps1` |
| P1 webhook reachable + https + fail-closed when unconfigured | ✅ mp-ops + ops_slice tests |
| guardrails Windows port | ✅ `ops/ci/guardrails.ps1` (green this session; **untracked** — not yet committed) |

**All safe 08-04 findings are closed in code.** Remaining owner-gated items from
that audit stay open by design (live/paper venue adapters, mechanical
`Mode::Live` barrier, real P1 credentials).

---

## System map (what exists)

```
collectors  →  core event log  →  storage (audit/compact/materialize)
                                    ↓
features ←→ strategies ←→ sim (backtest/paper) ←→ risk gate ←→ oms (state only)
                                    ↓
                              ops (opsd, alerts, pipeline, watchdog)
                                    ↓
                              llm + research/ (briefs, grading)
```

| Layer | Maturity | Notes |
|---|---|---|
| Event schema / log | Production-grade | Schema v2, torn-tail recovery, provenance |
| Collectors (Binance) | Live recording | BTCUSDT + ETHUSDT only today; multi-venue adapters exist |
| Integrity / scorecards | Automated daily | Gate honest: 0 clean days |
| Features / catalog / footprint | Implemented + tested | Specs 004/025 |
| Sim / funnel / risk / OMS | Implemented + tested | Paper path exists; **no live adapter** |
| Ops / dead-man / Telegram | Implemented | P1 webhook dead-until-creds (loud) |
| LLM providers | Fixture-tested | Keys from env only |
| Research Python | 49 tests green | Needs `py -3` on this host |
| Cold store / promotion | Gated correctly | Refuses dirty — correctly blocked |

---

## Operational health (Phase 0 — the real gate)

### Promotion gate (authoritative)

```text
mp-ops promote → {"consecutive_clean":0,"promoted":false,"required":7,
  "first_failure":"2026-07-18","summary":"NOT YET: 0 consecutive clean day(s)"}
```

**Phase-0 validation gate is not met.** Capital at risk correctly remains **$0**.

### Scorecard trend (honesty improving, still dirty)

| Date | avg coverage | blocking findings | promotable |
|---|---:|---:|---|
| 2026-07-18 … 07-31 | mixed / often low | **hundreds of thousands – millions** | no |
| 2026-08-03 | ~0.96 | 71 | no |
| 2026-08-04 | ~0.99 | 38 | no |
| 2026-08-05 | ~0.99 | **103** | no |

Recent days are *much* cleaner (recv-monotonic + stall fixes working), but every
day still fails INT-4. Spot audit of `20260805` binance/BTCUSDT:

- **1,418,655 events**, coverage **0.9900**
- **59 blocking findings**, almost all `sequence_gap` (book `pu` continuity)
- plus `coverage_gap`, `stale_stream`
- Live traces show repeated `Connection reset without closing handshake` →
  reconnect → REST reseed after `pu` gap (COL-24)

**Root cause of gate failure today is real stream instability / sequence gaps,
not a broken auditor.** The auditor is doing its job.

### Live collectors (this host, session time)

| Item | State |
|---|---|
| Processes | 2× `mp-collector` (PIDs 24404 BTC, 19620 ETH), started ~15:55 local |
| Heartbeats | fresh (~seconds old) |
| Today's logs | ~1.43 GiB so far (BTC ~784 MiB, ETH ~748 MiB), still growing |
| Watchdog earlier | restart storm (#29–#34) around 07:36–07:43Z; restarted cleanly 09:24Z |
| Disk free | **67.4 GiB** on C: |
| Raw corpus | **~15.8 GiB** under `data/raw` |

### Configuration drift (High)

`ops/core_symbols.txt` lists **8** Phase-0 symbols
(BTC/ETH/SOL/BNB/XRP/DOGE/ADA/LINK). Both watchdog and `daily_pipeline.ps1`
load that file as the single source of truth.

**Reality:** only BTCUSDT + ETHUSDT are recording and appear on scorecards.
Watchdog log: `Symbols: BTCUSDT,ETHUSDT`.

If the pipeline next runs with the default 8-symbol set while only 2 are
recorded, every day will fail for *missing* symbols even if BTC/ETH go clean.
**Recorded ≠ required** until either:

1. Watchdog is restarted to collect all 8 (≈3.5 GiB/day — needs free disk + backup), or
2. `core_symbols.txt` is trimmed back to the 2 symbols actually recorded.

ROADMAP already notes this; it remains the #1 operational inconsistency.

### Data location (High — W-6)

Workspace path:

`C:\Users\thwin\Downloads\Money-printer-…\…`

README / `core_symbols.txt` / W-6 explicitly warn: **do not keep compounding
market data under Downloads**. ~16 GiB of irreplaceable raw is on a disposable
OS path with no evidence of an off-host backup in this audit.

---

## New / residual findings

### Critical
**None** that enable remote compromise of funds on the default non-live config.

### High

1. **Phase-0 gate still at 0/7 clean days** — product goal unmet; driven by
   real `sequence_gap` / WS resets, not false positives.
2. **Data under `Downloads` + no verified backup** — single-disk loss risk
   for the entire compounding dataset (W-6).
3. **`core_symbols.txt` (8) vs live recorders (2)** — promotion/scorecard
   set can silently over-require; disk plan (~105 GiB/mo for 8) not provisioned
   (67 GiB free would not hold a month of 8-symbol raw).
4. **Dirty working tree / uncommitted deliverables** — `guardrails.ps1`,
   `mp-materialize` (+ tests), `ws-egress-filter.md`, and ~32 modified files
   (ops P1 webhook, materialize wiring, OMS/sim/llm tweaks). Loss of this
   session's work is a real risk; also means CI/other clones lack the Windows
   guardrails port.

### Medium

5. **L0 bar-fill still tagged `FillOptimism::None` while emitting
   `Liquidity::Maker`** (`sim/src/fills.rs` L0 path) — L0 is the most
   optimistic rung; G1 optimism accounting can under-count. Partial leftover
   from 08-02 H2 / 08-04 #11.
6. **No mechanical `Mode::Live` barrier** — env/file can select Live; safe
   only because no adapter exists yet. Must land before any venue trading code.
7. **`core` ring Disruptor payload protocol** — documented non-atomic
   overlapping access; fine for current `Copy` payloads under seq re-check;
   Miri would still flag. Acceptable debt, not silent.
8. **opsd unauthenticated on loopback without token** — by design (fail-closed
   off-loopback). Any local process can still POST `/beat/*` and silence
   dead-man if token unset. Prefer always-on token even on 127.0.0.1 for
   money-at-risk readiness.
9. **P1 webhook dead until credentials** — honest (exit 2); money-at-risk
   phone path still not live-operational.
10. **Early corpus provenance pollution** — `20260718` binance-named log
    audits as Hyperliquid `venue_mismatch` / `missing_provenance`. Do not use
    early days for promotion or research without quarantine.
11. **Collector restart / WS reset frequency** — trace shows multi-minute
    disconnect/reseed cycles; each contributes blocking `sequence_gap`s that
    keep days dirty even at 99% coverage.

### Low / hygiene

12. Nested clone path (`…/Money-printer-…/Money-printer-…/`) confuses tooling.
13. `python` launcher missing; research docs should say `py -3`.
14. Scorecard calendar gaps (e.g. 08-01, 08-02 missing) break consecutive-day
    counting even after first clean day appears.
15. `FeatureUpdate` still heaps `name: String` (spec 023 partial).
16. Clippy/workspace lints are good; `unsafe_op_in_unsafe_fn` is only `warn`.

---

## Uncommitted work inventory (do not lose)

| Path | Role |
|---|---|
| `ops/ci/guardrails.ps1` | Windows PD guardrails (pipeline already calls it) |
| `storage/src/materialize.rs` + `bin/mp-materialize.rs` + tests | Spec 016 materialization CLI |
| `ops/src/alert.rs`, `mp-ops.rs`, `ops_slice.rs` | P1 webhook wiring |
| `ops/runbooks/ws-egress-filter.md` | New runbook |
| assorted CLAUDE/AGENTS/specs/sim/oms/llm | Docs + small fixes |

**Recommendation:** commit in logical slices (ops P1 + guardrails; storage
materialize; docs) after a self-review pass — do not leave on a Downloads tree.

---

## What is genuinely strong

- Spec-first culture with mechanical guardrails and DOX hierarchy.
- Integrity gate is **honest** (refuses dirty cold writes; 0/7 reported truly).
- Risk gate, kill switches, OMS state machine, sim determinism, multi-strategy
  attribution — well tested.
- Collectors recover (reconnect, REST reseed, heartbeat watchdog) instead of
  silent death.
- Recent quality trajectory: blocking findings down ~4–5 orders of magnitude
  from mid-July to early August.
- 368 + 49 automated tests green; clippy deny-warnings clean.

---

## Recommended next steps (priority order)

### Do now (ops / Phase 0)

1. **Move `data/` + `data-migrated/` off Downloads** to a backed-up volume;
   verify restore once (`ops/restore-drill.sh` / backup script).
2. **Resolve symbol-set drift:** either collect all 8 core symbols (after disk
   headroom) **or** shrink `core_symbols.txt` to BTCUSDT+ETHUSDT until ready.
3. **Diagnose WS `sequence_gap` / connection resets** per
   `ops/runbooks/stream-gap.md` + new egress runbook — this is the only path
   to 7 clean days.
4. **Commit the dirty tree** (especially `guardrails.ps1` and materialize).
5. Ensure daily pipeline task is healthy through DST (one skip logged
   2026-08-05 04:04Z).

### Soon (hardening, still $0 capital)

6. Always require `MP_OPSD_TOKEN` even on loopback.
7. Tag L0 fills with an honest optimism enum (or strip L0 from G1 paths).
8. Spec/follow-up: compile- or init-time refusal of `Mode::Live` until an
   explicit owner-built adapter feature flag exists.
9. Quarantine pre-provenance / mixed-venue early logs from research inputs.

### Owner-gated (do not implement in agent sessions)

10. Provision real Telegram / P1 webhook credentials.
11. Paper/live venue adapters only after Phase 0 gate + written evidence.
12. Mechanical Live barrier + WAL journaled execution (prior audit backlog).

---

## Verdict

The **codebase is in strong shape**: builds, tests, clippy, and guardrails are
green; prior audit fixes hold; Prime Directives are structurally respected;
capital at risk is correctly **$0**.

The **product is not yet at the Phase-0 finish line**. Zero consecutive clean
days, data on a disposable path, symbol-set config drift, and ongoing Binance
WS sequence gaps are the binding constraints — not missing strategy cleverness.

Treat this as an **ops and data-integrity problem first**, not a feature-build
problem. Ship clean multi-day recordings on a durable disk; everything else in
`POST_CLEAN_PLAN.md` is already waiting behind that gate.

| Dimension | Grade |
|---|---|
| Code quality / tests | A |
| Safety / PD compliance | A− (Live barrier still conventional) |
| Ops tooling | A− (P1 creds + always-on opsd token open) |
| Phase-0 data integrity | **D+** (improving, gate unmet) |
| Durability / backup posture | **D** (Downloads, ~16 GiB, 67 GiB free) |
| Overall readiness for paper | Not yet — finish Phase 0 first |
| Overall readiness for live | Hard no (by design) |

---

*Auditor: Grok full-system pass, 2026-08-06. Evidence: cargo/pytest/clippy/
guardrails/promote/mp-audit outputs and live heartbeats captured in-session.*

---

## Full-corpus integrity audit — closeout (re-run 2026-08-07)

Re-ran the full-corpus per-day integrity audit over **all recordings in
`data/raw`** (`mp-audit --data-dir data --json`, binary rebuilt from the
current tree): 37 binance logs across **19 dates (07-18 → 08-07)** plus the
early bybit/hyperliquid experiment logs (07-19 ×7, 07-21 ×4). Fresh numbers
were cross-checked against the archived daily scorecards — **coverage matches
to the decimal** (08-05: 1,418,655 events, coverage 0.989991; 07-29:
0.129047). Per-finding *counts* differ slightly on 08-05 (archived 60/43 vs
fresh 42/59 per symbol) because that card was produced by an earlier pipeline
binary; coverage and the not-promotable verdicts agree.

### Verdict: **0 of 19 days promotable. Promotion gate: 0 consecutive clean days (need 7). First failure 2026-07-18.**

```text
mp-ops promote → {"consecutive_clean":0,"first_failure":"20260718",
  "promoted":false,"required":7,"window_start":null,"window_end":null}
```

| Date | Recordings | Coverage (BTC/ETH) | Blocking findings (dominant codes) |
|---|---|---|---|
| 07-18 | 1 (BTC) | 1.0000 | missing_provenance/symbol_mismatch/venue_mismatch ×371 |
| 07-19 | 7 (binance + bybit/hyperliquid exp.) | 0.72–1.00 | missing_provenance ×1.18M, symbol_mismatch ×68k (recv_time_reversal warn) |
| 07-20 | 1 (BTC) | 0.9977 | missing_provenance ×2.73M (recv_time_reversal warn) |
| 07-21 | 4 (binance + hyperliquid exp.) | 0.69–0.95 | missing_provenance ×1.48M, venue_mismatch ×381k, missing_snapshot_source ×338k |
| 07-22 | 3 (BTC/ETH/SOL) | 0.98 | missing_provenance ×2.57M, venue_mismatch ×980k, missing_snapshot_source ×825k |
| 07-23 | 2 | 0.965 | missing_provenance ×1.81M, venue_mismatch ×694k, missing_snapshot_source ×579k |
| 07-24 | 2 | 1.00/0.983 | missing_provenance ×1.46M, venue_mismatch ×559k, missing_snapshot_source ×468k |
| 07-25 | 2 | 0.971/0.967 | missing_provenance ×774k, sequence_gap ×261k, missing_snapshot_source ×271k |
| 07-26 | 2 | **0.56** | missing_provenance ×1.14M, venue_mismatch ×411k, missing_snapshot_source ×378k |
| 07-27 | 2 | 0.9977 | missing_provenance ×2.72M, venue_mismatch ×1.10M, missing_snapshot_source ×841k |
| 07-28 | 2 | 0.9708 | missing_provenance ×2.60M, venue_mismatch ×1.00M, missing_snapshot_source ×824k |
| 07-29 | 2 | **0.129/0.642** | sequence_gap ×217k, missing_provenance ×46k, venue_mismatch ×17k |
| 07-30 | 2 | 0.989/1.00 | sequence_gap ×749k (recv_time_reversal warn) |
| 07-31 | 2 | 0.9977 | sequence_gap ×774k (recv_time_reversal warn) |
| 08-03 | 2 | 0.977/0.948 | sequence_gap ×59, coverage_gap ×2, stale_stream ×2 |
| 08-04 | 2 | 0.9961 | sequence_gap ×78, coverage_gap ×2, stale_stream ×2 |
| 08-05 | 2 | 0.9900 | sequence_gap ×97, coverage_gap ×2, stale_stream ×2 |
| 08-06 | 2 | 0.978/0.979 | sequence_gap ×80, coverage_gap ×2, stale_stream ×2 |
| 08-07 (partial, in progress) | 2 | **1.0000** (provisional) | sequence_gap ×5, stale_stream ×2 |

### Why — the same three eras, one new data point each

- **Era 1 — 07-18 → 07-29: structurally unpromotable (legacy schema).** Every
event carries `missing_provenance`, `symbol/venue_mismatch`,
`missing_snapshot_source` — findings the audit emits when events lack the
provenance/metadata that spec 001/024 now requires. Permanent: no repair,
only re-recording. (07-19/07-21 include the one-off bybit/hyperliquid
experiments; 07-29's BTCUSDT lost 87% coverage.)
- **Era 2 — 07-30 → 07-31: collector-respawn sequence-gap storms.**
Provenance clean, but ~750k `sequence_gap`/day (~5/sec) from the pre-fix
collector that crash-respawned 22–45×/day. Recorded by a fixed binary; not
repairable retroactively.
- **Era 3 — 08-03 → 08-07: post-fix, modern schema, still dirty.** Blocking
counts are down to **5–97/day** (vs millions in July): `sequence_gap` from
WS resets + `coverage_gap`/`stale_stream` ×2 each. **New since the earlier
pass:** 08-06 is now fully recorded and scored (0.978/0.979, 84 blocking)
and 08-07 is recording at **coverage 1.0000 so far** — the closest any day
has come, but a partial day with 7 blocking findings is still not promotable.

### Egress filter still active (confirmed 08-07)

The `streams` map on every scored day confirms the spec 024 geo-filter is
still dropping the WS non-book streams: `mark_price`/`funding` sit at REST
poll cadence (4,488/day on 08-05 → 5,354/day on 08-06, the 15s poll) and
**`forceOrder` appears on no day** (it has no REST fallback, so its absence is
definitive). Trade volume arrives via the `trade_source=rest` fallback.
**The egress proxy remains the only path to a clean day** (`set_ws_proxy.ps1`
+ `ops/runbooks/ws-egress-filter.md`; verification: `forceOrder` in the audit
`streams` map + `mark_price` trending to ~86,400/day).

### Gate math

Even if a fully clean day appeared today, promotion needs **7 consecutive
clean days** → earliest finish ~2026-08-14. The 08-01/08-02 calendar gap
(recorder down) is already past; the streak counter is date-based, so no
further gap blocks counting once day one is clean.

*Re-run: `mp-audit` rebuilt from HEAD + full `--json` sweep, 2026-08-07.
Corpus now 37 binance recordings / 19 dates; 0 promotable.*
